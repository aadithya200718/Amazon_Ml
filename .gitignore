# Competition datasets and large archives (> 100MB GitHub limit)
6ab10eb3b23ba_student_resource/
*.zip
*.tar.gz

# Candidate pairs file exceeds GitHub's 100MB hard limit (689 MB)
# Can be regenerated locally via: python -m src.predict
team_submission/output/candidate_pairs.tsv
output/candidate_pairs.tsv

# Python cache and environment
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
env/
.DS_Store
